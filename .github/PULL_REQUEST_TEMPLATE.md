## Change

Describe the user-visible outcome and the concrete failure it prevents.

## Verification

- [ ] Relevant tests pass on Windows CPython 3.10.
- [ ] `python scripts/package_public.py --check` passes.
- [ ] No keys, tokens, usernames, absolute local paths, research content, databases, or runtime outputs are included.
- [ ] User-facing behavior or limitations are documented when changed.
